node_modules/
.env
.env.local
.env.*.local
.env.bak.*

# Per-order temporary wallet keys (NEVER commit — funds live here until swept)
.keys/

# Bot-side operational state (orders, post-ids, pump/trending dedup)
data/

*.log
npm-debug.log*

# GramJS user session (Telegram auth credentials — NEVER commit)
session.txt
*.session
